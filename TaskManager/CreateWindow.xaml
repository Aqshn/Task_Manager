<Window x:Class="TaskManager.CreateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManager"
        mc:Ignorable="d"
        Title="CreateWindow" Height="300" Width="500" WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Enter process name:" Margin="0,0,0,10"/>
            <TextBox x:Name="CreateTextBox" Width="200" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="CancelButton" Click="CancelButton_Click" Content="Cancel" Width="80" Margin="5,0"/>
                <Button x:Name="CreateButton" Click="CreateButton_Click" Content="Create" Width="80" Margin="5,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
